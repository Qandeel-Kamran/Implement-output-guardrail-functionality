[project]
name = "assisment-001"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.116.1",
    "openai-agent>=0.1.0",
    "python-decouple>=3.8",
    "requests>=2.32.5",
]
